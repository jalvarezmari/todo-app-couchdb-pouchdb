Esta es la TodoApp con estado y bien estructurada con funciones para hacer el CRUD.
Hay que añadirle PouchDb y sincronizar con CouchDB remoto.

En mi caso añadí una etiqueta div con el sync-wrapper para referencias el syncDom de app.js y agregué un fichero server.js

Arrancar con node server.js